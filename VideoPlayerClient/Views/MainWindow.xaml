<Window x:Class="VideoPlayerClient.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding MainWindowViewModel, Source={StaticResource Locator}}">

    <Grid>
        <DockPanel LastChildFill="False">
            <Image Source="/Views/1029845290_0_52_2000_1183_600x0_80_0_0_fdf366ed7d4682f429f40c1a4398c3c7.jpg" Width="562" />
            <DockPanel >
                <ComboBox Width="100" DockPanel.Dock="Top" Height="20" 
                          ItemsSource="{Binding CamerasIds}"
                          SelectedValuePath="Key" 
                          DisplayMemberPath="Value"
                          SelectedValue="{Binding SelectedCam}"/>
                <Button Content="Выбрать" DockPanel.Dock="Bottom" Height="20" Command="{Binding GetVidCommand}"/>
            </DockPanel>

        </DockPanel>

    </Grid>
</Window>
